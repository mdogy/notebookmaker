# NotebookMaker Environment Configuration
# Copy this file to .env and fill in your API keys
# NEVER commit the .env file to version control

# ==============================================================================
# LLM API Keys
# ==============================================================================
# You only need to set keys for the providers you plan to use.
# The application will auto-discover credentials from:
#   1. Environment variables (highest priority)
#   2. CLI credential files (Google Cloud, Anthropic CLI)
#   3. This .env file
#   4. Runtime prompt (fallback)

# Anthropic Claude
# Get your key from: https://console.anthropic.com/
# Or set via: export ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=

# OpenAI
# Get your key from: https://platform.openai.com/api-keys
# Or set via: export OPENAI_API_KEY=sk-...
OPENAI_API_KEY=

# Google AI Studio (Gemini)
# Get your key from: https://makersuite.google.com/app/apikey
# Or use Google Cloud ADC: gcloud auth application-default login
# Or set via: export GOOGLE_API_KEY=...
GOOGLE_API_KEY=

# OpenRouter
# Get your key from: https://openrouter.ai/keys
# Or set via: export OPENROUTER_API_KEY=sk-or-...
OPENROUTER_API_KEY=

# ==============================================================================
# Alternative: Use CLI Credentials (Recommended)
# ==============================================================================
# Instead of storing keys in .env, you can use existing CLI credentials:
#
# For Google Cloud:
#   gcloud auth application-default login
#
# For Anthropic (if using Claude CLI):
#   export ANTHROPIC_API_KEY=sk-ant-...
#
# For OpenAI:
#   export OPENAI_API_KEY=sk-...
#
# This approach is more secure as keys aren't stored in files.

# ==============================================================================
# LLM Configuration (Optional)
# ==============================================================================
# Default provider to use (anthropic, google, openai, openrouter)
DEFAULT_LLM_PROVIDER=anthropic

# Default model for each provider
ANTHROPIC_DEFAULT_MODEL=claude-sonnet-4-5-20250929
GOOGLE_DEFAULT_MODEL=gemini-2.0-flash-exp
OPENAI_DEFAULT_MODEL=gpt-4o
OPENROUTER_DEFAULT_MODEL=anthropic/claude-sonnet-4

# Default temperature (0.0-2.0)
DEFAULT_TEMPERATURE=0.7

# Default max tokens
DEFAULT_MAX_TOKENS=4096
